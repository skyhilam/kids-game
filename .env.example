# Optional server fallback. Users can instead paste a temporary token in the workshop.
# Copy to .env.local and restart npm run dev only if configuring this fallback/model.
# Never use a VITE_ prefix for this key.
OPENAI_API_KEY=
OPENAI_IMAGE_MODEL=gpt-image-2.5-sunburst
